<h1>Анкеты</h1>

<table>
  <thead>
    <tr>
      <th>Пол</th>
      <th>Возраст</th>
      <th>Язык</th>
      <th>Специальность</th>
      <th>Дата</th>
      <th>Город</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @worksheets.each do |worksheet| %>
      <tr>
        <td><%= worksheet.sex %></td>
        <td><%= worksheet.age %></td>
        <td><%= worksheet.language_name %></td>
        <td><%= worksheet.specialty_name %></td>
        <td><%= worksheet.dateinput %></td>
        <td><%= worksheet.city_name %></td>
        <td><%= link_to 'Show', worksheet %></td>
        <td><%= link_to 'Edit', edit_worksheet_path(worksheet) %></td>
        <td><%= link_to 'Destroy', worksheet, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Создать Анкету', new_worksheet_path %>
